FROM python:3.14-slim

WORKDIR /app

COPY pyproject.toml .
COPY src ./src
COPY alembic ./alembic
COPY alembic.ini .
RUN pip install --no-cache-dir -e .

CMD ["sh", "-c", "alembic upgrade head && exec uvicorn roster_balance.main:app --host 0.0.0.0 --port 8000"]
